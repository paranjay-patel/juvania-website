---
interface Props {
	class?: string;
	showText?: boolean;
}

const { class: className = "", showText = true } = Astro.props;
---
<div class="flex items-center">
	<span class={`font-poppins font-bold text-2xl tracking-tight text-primary ${className}`}>
		Juvania
	</span>
</div>
